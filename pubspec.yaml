name: tanapa
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  animations: ^2.0.7
  cloud_firestore: ^4.4.3
  firebase_core: ^2.7.0
  flutter:
    sdk: flutter
  get: ^4.6.5
  google_fonts: ^4.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/
